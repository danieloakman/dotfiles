{
  "name": "cursor-agent-http",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev":"CURSOR_API_KEY=$(cat /run/secrets/cursor_api_key) PORT=8333 bun run --watch server.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
