{
  "name": "opencode-cursor-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit && node build-test.mjs",
    "build": "node build.mjs",
    "test": "node build-test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
